<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
      </h1>
      <p>Name: <%= @user.name %></p>
      <p>Balance: &dollar;<%= @user.balance %></p>
    </section>
  </aside>
</div>